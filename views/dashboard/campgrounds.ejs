<h3 class="mb-2">My Posted Spots</h3>
<p class="text-muted"><%= totalCampgrounds %> total <%=totalReviews.length > 1 ? 'spots' : 'spot' %> </p>

<%- include('../partials/selectableGrid', {
  items: campgrounds,
  actionType: 'delete'
}) %>

<%- include('../partials/loadInfinityScroll', { 
  hasMore: hasMoreCampgrounds, 
  page, 
  tab 
}) %>
